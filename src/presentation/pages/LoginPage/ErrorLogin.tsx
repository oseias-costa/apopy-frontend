import { ContainerErrorLogin } from "../../styles/PageStyles/acess.styles"

export const ErrorLogin = () => {
    return(
        <ContainerErrorLogin>
            <p>Email ou senha incorreta.</p>
        </ContainerErrorLogin>
    )
}